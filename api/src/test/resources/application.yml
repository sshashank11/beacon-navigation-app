spring:
  datasource:
    url: jdbc:h2:mem:beacon;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none
management:
  health:
    redis:
      enabled: false
beacon:
  routing:
    enabled: false
