# Beacon Web

React and TypeScript frontend for Beacon, built with Vite, Tailwind CSS, MapLibre, TanStack Query, and Zustand.

## Development

```bash
npm install
npm run dev
```

Run `npm run build` for a production build and `npm run lint` for static checks.
